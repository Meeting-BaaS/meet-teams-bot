# Default deployment environment
ENVIRON="${ENVIRON:-preprod}"

# Scaleway registry
SCW_REGISTRY_HOST="${SCW_REGISTRY_HOST:-rg.fr-par.scw.cloud}"

# Image defaults
TARGET_ARCH="${TARGET_ARCH:-amd64}"

# GitOps config, used later
GITOPS_REPO_URL="${GITOPS_REPO_URL:-}"
GITOPS_BRANCH="${GITOPS_BRANCH:-main}"
